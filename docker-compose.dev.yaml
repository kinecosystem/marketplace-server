version: "3"

services:

  marketplace-server:
    image: kinecosystem/marketplace-server:latest
    volumes:
      - .:/opt/app
  marketplace-internal:
    image: kinecosystem/marketplace-server:latest
    volumes:
      - .:/opt/app
